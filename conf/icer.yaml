hydra:
  run:
    dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
defaults:
  - base_config
  - data: icer
  - spectrogram: sample
  - model: sample
  - train: icer
  - profile: sample
  - visualize: icer
  - _self_
