hydra:
  run:
    dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
defaults:
  - base_config
  - data: icer
  - spectrogram: icer
  - model: icer
  - train: icer
  - visualize: icer
  - _self_
