hydra:
  run:
    dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
defaults:
  - base_config
  - data: icer_all
  - spectrogram: icer_all
  - model: icer_all
  - train: icer
  - profile: sample
  - visualize: icer
  - _self_
